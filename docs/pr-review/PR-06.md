# LIIT 06 — Creator mode, event operations, content, analytics, and payouts

## Plan

1.  **Domain & Repository**: 
    - Define domain models in `src/domain/creator/index.ts`.
    - Create fixtures in `src/fixtures/creator/`.
    - Create `MockCreatorRepository.ts` in `src/repositories/mock/`.
    - Create hooks in `src/hooks/creator/`.

2.  **Shared Components**:
    - Build `CreatorStatCard`, `EventStatusPill`, `EventCreatorRow`, `AnalyticsChart`, `PayoutSummaryCard`, `ContentPostRow` in `src/components/creator/`.

3.  **Shell & Navigation**:
    - Update `app/(creator)/_layout.tsx` for Dashboard, Create, Events, Tools, Profile tabs.

4.  **Core Screens**:
    - Implement `dashboard.tsx`, `create.tsx`, `events.tsx`, `tools.tsx`, `profile.tsx` under `app/(creator)/(tabs)/`.

5.  **Nested & Modal Routes**:
    - `activation.tsx`, `verification.tsx`.
    - `events/[eventId].tsx` and sub-routes (`edit`, `preview`, `analytics`, `guests`, `content`).
    - `payouts.tsx`, `notifications.tsx`.
    - `publish-confirmation.tsx`, `request-payout.tsx` under `app/(modals)/`.

6.  **Tests**:
    - Create test files: `Instruction6CreatorDomain.test.ts`, `Instruction6DashboardRendered.test.tsx`, `Instruction6EventsListRendered.test.tsx`, `Instruction6CreatorRoutes.test.ts`.

7.  **Verification**:
    - Run formatting, type checking, linting, and tests. Update PR with results.
